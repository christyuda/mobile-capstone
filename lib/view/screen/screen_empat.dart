import 'package:flutter/material.dart';

class ScreenEmpat extends StatelessWidget {
  const ScreenEmpat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("screen empat"),
      ),
    );
  }
}
