import 'package:flutter/material.dart';

class ScreenLima extends StatelessWidget {
  const ScreenLima({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("screen lima"),
      ),
    );
  }
}
